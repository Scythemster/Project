# EventForge Frontend Environment
# The frontend talks to the backend via the /api proxy configured in vite.config.ts.
# No secrets belong here — API keys live only in the backend.
VITE_API_BASE=/api
